<h1>New project_event</h1>

<% form_for([:admin, @project_event]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_project_events_path %>
