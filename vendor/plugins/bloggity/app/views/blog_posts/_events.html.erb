				<!-- next event -->
				<div class="box-rightcol-top"></div>
				<div class="box-rightcol-inner">
					<div class="header"><h4>NEXT EVENT</h4></div>
          <div id="events">
            <%= render :partial => "event", :collection => (events = @project.events.top_4.all), :locals => { :last => events.last } %>
          </div>
					<%= link_to_remote "view more", { :url => events_project_path(@project), :with => "'page=' + ++events_page", :method => :get }, :id => "home_news_items_view_more", :class => "pad_top pad_left" %>
				</div>
				<div class="box-rightcol-bottom"></div>
<% javascript_tag do %>
  events_page = 1;
<% end %>
